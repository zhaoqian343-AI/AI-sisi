# Sisi

一个可直接打开的静态网页：`index.html` + `styles.css` + `script.js`。

## 本地预览

用浏览器打开 `index.html`，或在项目目录运行：

```bash
python3 -m http.server 8080
```

然后访问 `http://localhost:8080`。

## 页面里有什么

- 首屏主视觉与主按钮
- 「关于」说明区块
- 「打个招呼」会切换欢迎文案
